// AWS configuration constants — safe to hardcode non-secret values here.
// Sensitive credentials (pool IDs, client IDs) come from app.json extra → Amplify.configure().
export const AWS_REGION = 'us-east-1';
